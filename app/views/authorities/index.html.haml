%p#notice= notice
%h1 Authorities
%table
  %thead
    %tr
      %th Name
      %th Email
      %th Website
      %th Sign key pem
      %th Encrypt key pem
      %th{:colspan => "3"}
  %tbody
    - @authorities.each do |authority|
      %tr
        %td= authority.name
        %td= authority.email
        %td= authority.website
        %td= authority.sign_key_pem
        %td= authority.encrypt_key_pem
        %td= link_to 'Show', authority
        %td= link_to 'Edit', edit_authority_path(authority)
        %td= link_to 'Destroy', authority, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Authority', new_authority_path
